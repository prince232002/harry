# import array as arr
# arr.array()

from array import * # here * means all function uses inside array
vals=array('i',[4,56,2,-2])
vals.reverse()
for e in range(len(vals)):
    print(e)
for i in vals:
    print(i)
"""
array is similar to list but we can store element of same datatype
"""
